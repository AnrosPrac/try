#include <stdio.h>
#include<math.h>
#define square(x) (x*x)
int main() {
int r;
    printf("enter the number to be squared:\n");
    scanf("%d",&r);
int t=square(r);
    printf("squared numbered=%d",t);

    
}